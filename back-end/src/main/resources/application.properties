# Nome da aplica��o
spring.application.name=eventos

# Configura��o da conex�o com o banco de dados PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/papillon
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# Configura��es do Hibernate (JPA)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Zona de hor�rio da aplica��o
app.zonedatetime=America/Sao_Paulo

# Mostrar os comandos SQL no console
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Log de bind dos par�metros (mostra os valores que s�o passados para cada ???)
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Log s� do SQL gerado
logging.level.org.hibernate.SQL=DEBUG



